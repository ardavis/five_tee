%h1 Completed Tasks
- @completed_tasks.each do |task|
  .row.well
    .col-md-4
      = link_to task.title, show_task_modal_path, format: :js, remote: true, task: task

    .col-md-4
      Time took:
      = duration_display(task.duration)


    .col-md-4
      Finished
      = finished_display(task.completed_at)
      .pull-right
        = link_to restart_task_path(task), class: 'btn btn-default', format: :js, remote: true do
          %span.glyphicon.glyphicon-arrow-up